/*
 * There are two different parts in object
 * 1. Noun / Adjective (State / data / property)
 * 2. Verb ( Functionalities -> start, stop)
 *
 *
 * Object property -> keys, values, freeze, create, assign, entries
 * Object.keys(microphone) - return an array from object keys
 * Object.values(microphone) - return an array from object values
 * Object.entries(microphone) - return an array with key and value
 * Object.freeze(microphone) - it will not accept new property or method
 *
 *
 * for ... in loop
 * dot notation microphone.brand
 * array notation microphone['brand']
 *
 */
